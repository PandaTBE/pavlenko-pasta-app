import { LoginCard } from '@/widgets/login-card';

export const Login = () => {
    return (
        <div className="flex justify-center container !max-w-none">
            <LoginCard />
        </div>
    );
};
