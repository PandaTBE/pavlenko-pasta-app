import { RegisterCard } from '@/widgets/register-card';

export const Register = () => {
    return (
        <div className="flex justify-center container !max-w-none">
            <RegisterCard />
        </div>
    );
};
